<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.arvin.dao.CollectMapper" >
    <resultMap id="BaseResultMap" type="com.arvin.pojo.Collect" >
        <constructor >
            <idArg column="id" jdbcType="INTEGER" javaType="java.lang.Integer" />
            <arg column="product_id " jdbcType="INTEGER" javaType="java.lang.Integer" />
            <arg column="user_id  " jdbcType="INTEGER" javaType="java.lang.Integer" />
            <arg column="create_time" jdbcType="TIMESTAMP" javaType="java.util.Date" />
        </constructor>
    </resultMap>
    <sql id="Base_Column_List" >
        id, product_id,user_id,create_time
    </sql>

    <select id="selectCollect" parameterType="com.arvin.pojo.Collect" resultType="int">
        select count(*)
        from arvin_collect
        where product_id = #{productId} and user_id = #{userId}
    </select>
    <insert id="insertCollect" parameterType="com.arvin.pojo.Collect" >
        insert into arvin_collect (product_id,user_id,create_time) values (#{productId},#{userId},now())
    </insert>
    <select id="selectCollectList" parameterType="int" resultType="map">
        select c.user_id,DATE_FORMAT(c.create_time,'%Y-%m-%d %H:%i:%s') as create_time,p.id as productId,p.main_image,p.name,p.price from (select
        * from arvin_collect where user_id = #{userId} ) c left join
        arvin_product p on p.id = c.product_id;
    </select>
    <delete id="deleteCollect" parameterType="com.arvin.pojo.Collect">
        DELETE
        from arvin_collect
        where user_id = #{userId} and product_id = #{productId}
    </delete>



</mapper>